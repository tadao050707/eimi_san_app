 <%= form_with model: @user_choice, url: user_steps_path, method: :post do |form| %>
  <h6 class="which_one">当てはまる方を選んでください</h6>
  <p class="enum-choice"><%= t('.About house')%></p>

  <div class="enum-choice">
    <%= form.radio_button :house, :owned, class: "radio-inline__input" %>
    <%= form.label(:house, "owned", value: "owned", class: "radio-inline__label") %>
    <%= form.radio_button :house, :lent, class: "radio-inline__input" %>
    <%= form.label(:house, "lent", value: "lent", class: "radio-inline__label") %>
  </div>
  
  <div class="actions">
    <%= form.submit "次へ", class: "button004"%>
  </div>
  <div class="step_back">
    <%= link_to 'Back', fifth_user_steps_path %>
  </div>

  <div class="tip">
    <p>お家に帰ったら、わんちゃんが待っている暮らし、素敵ですよね。</p>
    <p>賃貸で、ペット可の物件が少なかったり、</p>
    <p>小型犬のみとサイズが指定されていることが多いです。</p>
    <p>飼いたい犬種の成犬時のサイズの確認は大切ですね。</p>
  <div>
<% end %>